╔══════════════════════════════════════════════════════════════════════╗
║                  CONVEX ENVIRONMENT VARIABLES SETUP                   ║
║                        ⚠️  ACTION REQUIRED ⚠️                         ║
╚══════════════════════════════════════════════════════════════════════╝

📍 WHERE TO GO:
   https://dashboard.convex.dev
   → Select: hands-off-ur-keyboard
   → Navigate to: Settings → Environment Variables

📝 WHAT TO ADD:
   Get your API keys from the services below, then add to Convex:

┌──────────────────────────────────────────────────────────────────────┐
│ Key: GEMINI_API_KEY                                                  │
│ Value: <Get from https://aistudio.google.com/app/apikey>            │
│ Purpose: AI-powered search, voice command parsing, preference tags   │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│ Key: DAILY_API_KEY                                                   │
│ Value: <Get from https://dashboard.daily.co/developers>             │
│ Purpose: Real-time voice chat with AI assistant                      │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│ Key: BRIGHTDATA_API_KEY                                              │
│ Value: <Get from https://brightdata.com/cp/zones>                   │
│ Purpose: Real product data scraping (optional - uses mock if missing)│
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│ Key: BRIGHTDATA_MCP_ENDPOINT                                         │
│ Value: https://api.brightdata.com/mcp/v1/search                      │
│ Purpose: BrightData API endpoint (optional)                          │
└──────────────────────────────────────────────────────────────────────┘

🔄 AFTER ADDING:
   1. Click "Save" for each variable
   2. Wait ~30 seconds for Convex to redeploy
   3. Run: npm run dev
   4. Test at: http://localhost:3000

✅ VERIFICATION:
   • Frontend keys in .env.local: Configure from service dashboards
   • Convex backend keys: Add to Convex dashboard (steps above)
   • Daily.co domain allowlist: Add localhost:3000

🔒 SECURITY NOTE:
   NEVER commit API keys to git. Always use environment variables.
   Keep your .env.local file private (already in .gitignore)

📚 MORE INFO:
   See: /docs/setup/QUICK_START.md
